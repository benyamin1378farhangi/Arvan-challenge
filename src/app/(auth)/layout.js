// Centered auth shell (background, card container) is added in Phase 4
// once the Sign-in/Sign-up UI is implemented. Kept as a plain passthrough
// for now so the route group exists and is ready to receive that layout.
export default function AuthLayout({ children }) {
  return children;
}
